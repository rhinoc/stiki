import type { ThemeState } from "../types/tab";

export const DEFAULT_THEME: ThemeState = {
  color: {
    spice: {
      light: [0.46, 0.18, 260],
      dark: [0.78, 0.15, 171],
    },
  },
  powerMode: false,
};
